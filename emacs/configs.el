;; Bind ; to force normal mode or if in normal mode bind ; to enter ido mini buffer
;; I can acces ; by pressen H-;
;; Bind , to (
;;
;; Bind caps lock to hyper in normal mode in insertmode leave it to ctrl
;; Bind space in normalmode plugin specific behavior like smart parens and cider
