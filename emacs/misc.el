;;; Here i have useful tips and trics

;;C-j is used in IDO mode to create new file

;; M-: major-mode to get mode

;Use seil to bind caps lock to right control then this line will bind it to hyper
;(setq ns-right-control-modifier 'hyper)
