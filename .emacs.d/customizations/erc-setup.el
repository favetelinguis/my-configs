;; Autoload only erc-select command
;; (autoload 'erc-select "erc" "IRC client." t)
;; (require 'erc)
;; Makes new private messages appear in the minibuffer
(setq erc-echo-notices-in-minibuffer-flag t)
